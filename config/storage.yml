# The Disk service root is configuration, not a literal — see the path helpers
# in config/application.rb. Deployments that mount a volume elsewhere set
# VERSO_STORAGE_PATH; a fresh clone needs nothing.

test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Verso.storage_root %>
